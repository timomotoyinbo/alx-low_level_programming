#include "main.h"

/**
  * _abs - computes the abs value of an int
  * @n: check n for absolute value
  * Return: the abs of the int.
  */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
